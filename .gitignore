# Ansible vault files (encrypted secrets)
**/vault.yml
!**/vault.yml.example

# Host inventory (contains server IPs)
**/hosts.yml
!**/hosts.yml.example

# Retry files
*.retry

# Local inventory overrides
*.local.yml
